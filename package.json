{
  "name": "booky",
  "private": true,
  "version": "2.0.5",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=dev vite build --watch",
    "build": "tsc && vite build",
    "build:firefox": "NODE_ENV=production BROWSER=firefox vite build",
    "build:chrome": "NODE_ENV=production BROWSER=chrome vite build",
    "zip:chrome": "(cd dist; zip -r ../chrome.zip .)",
    "zip:firefox": "(cd dist; zip -r ../firefox.zip .)"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.217",
    "@types/node": "^18.14.0",
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "muuri": "^0.9.5",
    "staten": "^0.1.1"
  }
}
